import 'package:flutter/material.dart';

abstract class AppDecProp {
  AppDecProp._();
  static final defaultBorderRadius = BorderRadius.circular(12);
  static const defaultPadding = EdgeInsets.all(16);
}
