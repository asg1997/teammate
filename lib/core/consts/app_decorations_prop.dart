import 'package:flutter/material.dart';

abstract class AppDecorationProp {
  AppDecorationProp._();
  static final defaultBorderRadius = BorderRadius.circular(5);
  static const defaultPadding = EdgeInsets.all(16);
}
