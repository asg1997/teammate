import 'package:flutter/material.dart';

class RecoveredPasswordScreen extends StatelessWidget {
  const RecoveredPasswordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
